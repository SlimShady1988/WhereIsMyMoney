import React from 'react';
import {observer} from "mobx-react-lite";

const OperationDiagram = observer(() => {
    return (
        <div>
DIAGRAM
        </div>
    );
});

export default OperationDiagram;